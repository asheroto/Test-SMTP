# UTF-8
#
# Windows version resource for Test-SMTP.exe.
# PyInstaller reads this (via build.ps1 --version-file) and stamps it into
# the binary's file properties. To bump the version, change every number
# group below and the matching strings, and keep it in sync with
# __version__ in Test-SMTP.py.

VSVersionInfo(
    ffi=FixedFileInfo(
        filevers=(1, 0, 1, 0),
        prodvers=(1, 0, 1, 0),
        mask=0x3f,
        flags=0x0,
        OS=0x40004,
        fileType=0x1,
        subtype=0x0,
        date=(0, 0)
    ),
    kids=[
        StringFileInfo(
            [
                StringTable(
                    '040904B0',
                    [
                        StringStruct('CompanyName', 'asheroto'),
                        StringStruct('FileDescription', 'SMTP Connection Tester'),
                        StringStruct('FileVersion', '1.0.1.0'),
                        StringStruct('InternalName', 'Test-SMTP'),
                        StringStruct('LegalCopyright', 'Copyright (c) 2026 asheroto'),
                        StringStruct('OriginalFilename', 'Test-SMTP.exe'),
                        StringStruct('ProductName', 'SMTP Connection Tester'),
                        StringStruct('ProductVersion', '1.0.1.0')
                    ]
                )
            ]
        ),
        VarFileInfo([VarStruct('Translation', [1033, 1200])])
    ]
)